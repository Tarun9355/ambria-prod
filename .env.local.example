# Copy to .env.local and fill in. The CLIENT (Vite) only reads VITE_-prefixed vars via
# import.meta.env — see src/lib/supabase.js. Non-VITE_ vars here are for reference only
# (Edge Functions / CI), never bundled into the client.
VITE_SUPABASE_URL=https://taalribntdkowoqltvqw.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Server-side only (Supabase Edge Function secrets / CI) — NOT read by the client bundle:
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
CLOUDINARY_CLOUD=dy9wfqhry
CLOUDINARY_API_KEY=592743487577154
CLOUDINARY_SECRET=your_cloudinary_secret_here
