# Only pins per-function gateway settings that must survive `supabase functions deploy` —
# not a full `supabase init` config (no local dev stack config here).
#
# (The batch-tagger function and its verify_jwt override were removed — the nightly cron tagger
# is no longer part of the system. No per-function gateway overrides are currently needed.)
